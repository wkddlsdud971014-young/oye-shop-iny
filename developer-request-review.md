# GA4 이벤트 구현 요청 검토

## 검토한 요청

상품 상세 화면에서 「장바구니에 담기」 단추를 누른 순간 GA4로 `add_to_cart` 이벤트를 보낸다. 같이 보낼 값은 `currency`(KRW), `value`(가격 × 수량), `items`(`item_id`, `item_name`, `price`, `quantity`)다. 담기는 많은데 결제로 안 넘어가는 상품을 보고 장바구니 화면을 손볼지 정하려는 숫자다.

## 되물을 질문

| 질문 | 답이 필요한 이유 |
|---|---|
| GTM(dataLayer)으로 보내나요, gtag로 바로 보내나요? GTM이면 GA4 이벤트 태그와 트리거는 누가 만드나요? | 사이트에는 GTM만 깔려 있고 gtag는 없어서, 보내는 방법과 GTM 설정 담당이 정해지지 않으면 코드를 넣어도 GA4에 이벤트가 들어오지 않을 수 있다. |
| 같은 상품을 두 번째로 담을 때 `quantity`와 `value`에 이번에 담은 1개만 넣나요, 장바구니에 쌓인 전체 수량(2개)을 넣나요? | 상세 화면에는 수량을 고르는 칸이 없고 누를 때마다 1개씩 쌓이므로, 쌓인 수량을 넣으면 앞서 담은 것이 또 세어져 담은 수량과 금액이 부풀려진다. |
| 결제로 넘어갔는지는 어떤 이벤트로 보나요? `begin_checkout` 같은 이벤트도 이번에 같이 심나요? | 이번 요청은 `add_to_cart` 하나뿐이라, 결제로 넘어간 상품을 세는 이벤트가 없으면 담기는 많은데 결제로 안 넘어가는 상품을 가려낼 수 없다. |

## 받은 답

1. GTM(dataLayer)으로 보낼지 gtag로 바로 보낼지는 측정 계획서에 정해져 있지 않다. 확인 필요로 남긴다.
2. 같은 상품을 두 번째로 담아도 그 클릭으로 이번에 추가된 수량만 보낸다. 장바구니에 쌓인 전체 수량이나 전체 금액으로 바꾸지 않는다.
3. `begin_checkout`은 측정 계획서에 따로 정의된 이벤트다. 이번 요청에 넣지 않고 별도 구현 요청으로 다룬다.

## 고친 개발자 요청

상품 상세 화면에서 「장바구니에 담기」 단추를 눌러 실제로 상품이 장바구니에 추가된 경우에만 GA4로 `add_to_cart` 이벤트를 보내 주세요.
같이 보낼 값은 `currency`(KRW), `value`(담은 상품의 가격 × 이번에 추가된 수량), `items`(담은 상품의 `item_id`, `item_name`, `price`, `quantity`)입니다.
같은 상품을 여러 번 담아도 이벤트마다 그 클릭으로 이번에 추가된 수량만 보내고, 장바구니에 쌓인 전체 수량이나 전체 금액을 보내지 않습니다. 예를 들어 12,000원짜리 상품을 한 번 담으면 `quantity`는 1, `value`는 12000입니다.
담기는 많은데 결제로 안 넘어가는 상품을 보고 장바구니 화면을 손볼지 정하려는 숫자입니다.
`begin_checkout`은 이번 요청에 포함하지 않으며 별도 구현 요청으로 드립니다.

확인 필요: GTM(dataLayer)으로 보낼지 gtag로 바로 보낼지는 아직 정해지지 않았습니다.
